doctype html
html
  head
    title Dashboard
  body(class="bg-green-100 h-screen flex justify-center items-center flex-col")
    h1.text-3xl.font-bold Welcome to your dashboard!
    ul
      each text in store
        li #{text.id} - Text: #{text.text}